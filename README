Run main.py script with parameters in command line:
    'car' or 'iris' or 'adult' (dataset)
    80 (int, percent of training data)
    200 (int, maximum iterations )
    0.2 (float, learning rate)
    2 (int, number of hidden layers )
    4 (int, number of neurons in first hidden layer)
    2 （int, number of neurons in second hidden layer)

For example, run 'python3 main.py car 80 200 0.2 2 4 2'

Python version: 3
Dependencies:
    Pandas
    Numpy

